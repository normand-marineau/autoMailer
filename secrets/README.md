# secrets/

This folder is ignored by Git (except this README).

Put your Gmail OAuth files here (local only):

- credentials.json  (Google Cloud OAuth client)
- 	oken.json        (created after first successful login)

The app expects secrets/credentials.json and will write secrets/token.json.